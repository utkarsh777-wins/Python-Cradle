# Lists&Tuples
"""
#Lists
-A built-in data tyoe that stores set of values.
-It can store elements of different types(integer, float, string, etc.)
-Lists is kinda similar to strings
-Strings are immutable
-Lists are mutable

"""
marks1 = 94.4
marks2 = 87.2
marks3 = 77.7
marks4 = 66.6
marks5 = 99.9
# it isn't conventional to assign too many variables like this

marks = [] 
# hence is why we create lists

# indexing is valid in lists
# length --valid

                #----

# Tuples
# A built in data type that lets us create immutable sequence of values.
"""

#lists- mutable
#tuples- immutable

"""
tup = (2, 1, 3, 2)      #listing within pranthesis
print(tup[0])
print(tup[2])
# tup[0] = 5        3'tuple' object does not support item assignment
# since tuples are immutable
# slicing is valid in tuples